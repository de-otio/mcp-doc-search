# MCP Integration

MCP Doc Search exposes a [Model Context Protocol (MCP)](https://modelcontextprotocol.io) server that lets any MCP-compatible AI assistant search, list, and reindex your documentation directly. This works with Claude Code, Cursor, and any other client that supports MCP.

## Setup

### Step 1: Generate the configuration file

1. Open the command palette (`Cmd+Shift+P` / `Ctrl+Shift+P`)
2. Run **Doc Search: Generate .mcp.json**
3. A `.mcp.json` file is created in your workspace root

The generated file carries your effective settings in its `env` block and
looks like this:

```json
{
  "mcpServers": {
    "doc-search": {
      "command": "node",
      "args": ["${HOME}/.doc-search/bin/mcp-server.js"],
      "env": {
        "DOC_SEARCH_WORKSPACE": "${CLAUDE_PROJECT_DIR}",
        "DOC_SEARCH_GLOB": "doc/**/*.md",
        "DOC_SEARCH_EXTRA_ROOTS": "[{\"name\":\"vendor-docs\",\"path\":\"~/repos/vendor/docs\"}]"
      }
    }
  }
}
```

The file is **portable**: Claude Code expands `${HOME}` and
`${CLAUDE_PROJECT_DIR}` when it launches the server, so the same file works
for every user and checkout location. The `args` path is the **stable
launcher**: a tiny forwarder under `~/.doc-search/bin/` that the extension
refreshes on every activation to target the currently installed build, so the
config survives extension upgrades. (Configs generated by older versions embed
an absolute, versioned extension path instead; the extension repairs those to
the stable path when the workspace is next opened, and leaves the portable
form alone.)

The `env` block is what the server actually reads. It no longer takes
`extraRoots` or the embedding provider from the workspace's
`.vscode/settings.json` (see [Trust model](configuration.md#trust-model)), so
the generator writes them here:

- `DOC_SEARCH_EXTRA_ROOTS` — your `docSearch.extraRoots` setting as JSON
  (omitted when empty).
- `OLLAMA_URL` / `OLLAMA_MODEL` for the Ollama provider, or `USE_OPENAI=1`
  plus `"OPENAI_API_KEY": "${OPENAI_API_KEY}"` for OpenAI. The key itself is
  **never** written into the file — export `OPENAI_API_KEY` in the shell that
  starts Claude Code (for example in `~/.zshrc`) and Claude Code substitutes
  it at launch.

The file is written with mode `0600` and added to `.gitignore`. If
`.mcp.json` is already tracked by git in the workspace, the command warns
(without blocking) so you can `git rm --cached` it. Regenerate the file after
changing `extraRoots` or the provider; the server reads its env once at start.

### Step 2: Connect your MCP client

Point your MCP client at the generated `.mcp.json`. Most clients read it automatically from the workspace root.

**Claude Code** (VS Code extension or CLI): detects `.mcp.json` automatically. You will see a prompt to approve the project-scoped MCP server the first time.

**Claude Code CLI:**

```bash
claude mcp list   # verify doc-search appears
```

**Other clients:** refer to your client's documentation for how to load an `.mcp.json` or configure a stdio MCP server.

### Step 3: Verify it works

Ask your AI assistant something like:

> "Search my docs for getting started"

It will call the `search_docs` tool and return matching documentation sections.

### Troubleshooting

**"Tool not found" or the assistant doesn't use MCP tools**

- Make sure `.mcp.json` is in the **workspace root** (the folder you opened in your editor)
- Reload the window or restart the client
- For Claude Code CLI: run `claude mcp list` to check if `doc-search` appears

**"No results" from search**

- The search index must be built first. Run **Doc Search: Reindex Documentation** from the command palette before using the MCP tools
- The MCP server shares the same index as the VS Code extension — if search works in the extension, it will work via MCP

**Server fails to start**

- Check that the `args` path in `.mcp.json` points to a valid file. The
  stable launcher should exist at:
  ```bash
  ls ~/.doc-search/bin/mcp-server.js
  ```
  It is (re)written whenever the extension activates — open any workspace in
  VS Code once, or regenerate `.mcp.json`.
- If the config still embeds a versioned path
  (`…/de-otio.mcp-doc-search-X.Y.Z/dist/mcp-server.js`) from an older
  version, it breaks on every upgrade — regenerate `.mcp.json` (or open the
  workspace in VS Code, which repairs it to the stable path automatically)
- A client that does not expand `${HOME}` / `${CLAUDE_PROJECT_DIR}` cannot
  use the portable file as-is. The setup panel's tabs for other clients show
  the same config with absolute paths — copy that form instead. (If
  `DOC_SEARCH_WORKSPACE` reaches the server unexpanded, it warns and falls
  back to the current directory.)

**External roots or the Ollama/OpenAI provider stopped working via MCP**

- The server reads those from the `env` block only, not from
  `.vscode/settings.json` (its stderr says which keys it ignored). Regenerate
  `.mcp.json` so the current settings are written into `env`, or set
  `DOC_SEARCH_TRUST_WORKSPACE_SETTINGS=1` for a workspace whose settings you
  wrote yourself. See [Trust model](configuration.md#trust-model).

## Manual setup (without the extension command)

If you prefer to configure the MCP server manually:

### Creating .mcp.json by hand

Create `.mcp.json` in your workspace root:

```json
{
  "mcpServers": {
    "doc-search": {
      "command": "node",
      "args": ["${HOME}/.doc-search/bin/mcp-server.js"],
      "env": {
        "DOC_SEARCH_WORKSPACE": "${CLAUDE_PROJECT_DIR}",
        "DOC_SEARCH_GLOB": "doc/**/*.md",
        "DOC_SEARCH_EXTRA_ROOTS": "[{\"name\":\"vendor-docs\",\"path\":\"~/repos/vendor/docs\"}]"
      }
    }
  }
}
```

For a client other than Claude Code, write the launcher and workspace as
absolute paths (`<home>/.doc-search/bin/mcp-server.js`,
`<absolute-path-to-workspace>`). Prefer the stable launcher path either way —
it survives extension upgrades. Pointing directly at the extension install
works too but embeds the version number, so it breaks on every upgrade:

```bash
ls ~/.vscode/extensions/de-otio.mcp-doc-search-*/dist/mcp-server.js
```

Remember that external roots and the embedding provider are read from `env`
only; add `DOC_SEARCH_EXTRA_ROOTS`, `OLLAMA_URL` / `OLLAMA_MODEL` or
`USE_OPENAI=1` there as needed. Reference the OpenAI key as
`"${OPENAI_API_KEY}"` rather than pasting it, and `chmod 600` the file.

### Using the Claude Code CLI

```bash
claude mcp add doc-search \
  -s project \
  -e 'DOC_SEARCH_WORKSPACE=${CLAUDE_PROJECT_DIR}' \
  -e 'DOC_SEARCH_GLOB=doc/**/*.md' \
  -- node ~/.doc-search/bin/mcp-server.js
```

The single quotes keep the shell from expanding `${CLAUDE_PROJECT_DIR}` (or
`${OPENAI_API_KEY}`) at add time, so Claude Code stores the reference and
expands it at launch.

> Using an AI coding agent as the client? The [Agent Guide](agent-guide.md)
> covers token-efficient usage patterns — chunk-level retrieval, multi-repo
> federation via external roots, and delegating broad doc sweeps to
> subagents.

## MCP Tools

Once connected, the assistant can call three tools:

### search_docs

Search documentation using natural language.

**Parameters:**

| Name    | Type   | Default    | Description                         |
| ------- | ------ | ---------- | ----------------------------------- |
| `query` | string | (required) | Natural language search query       |
| `n`     | number | 5          | Maximum number of results to return |

**Returns:** Array of search results, each containing:

- `file` — relative path to the source file
- `heading` — the section heading
- `text` — chunk text content
- `lineStart` — line number in the source file
- `score` — relevance score (0-1, higher is better)

### list_docs

List all indexed documentation files.

**Parameters:** None

**Returns:** Array of `{file, title}` objects for every indexed file.

### reindex_docs

Trigger a documentation reindex.

**Parameters:**

| Name    | Type    | Default | Description                                               |
| ------- | ------- | ------- | --------------------------------------------------------- |
| `force` | boolean | false   | If true, reindex all files. If false, only changed files. |

**Returns:** Index statistics:

- `indexed` — number of files processed
- `skipped` — number of unchanged files skipped
- `totalChunks` — total chunks in the index
- `durationMs` — time taken in milliseconds

## Environment Variables

The MCP server is configured via the `env` block in `.mcp.json`. The **Generate .mcp.json** command fills these in automatically from your extension settings. Indexes are centralized under `~/.doc-search` (see [Configuration](configuration.md)), so no index-location variables are needed; the in-tree layout is deprecated, and `DOC_SEARCH_INDEX_LOCATION=workspace` exists only to keep it where unavoidable.

External roots and the embedding provider are **env-only**: the server does not read them from the workspace's `.vscode/settings.json` unless `DOC_SEARCH_TRUST_WORKSPACE_SETTINGS=1` is set, and an env value always wins over a settings.json value. See [Trust model](configuration.md#trust-model).

| Variable                              | Default             | Description                                                                                 |
| ------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------- |
| `DOC_SEARCH_WORKSPACE`                | `process.cwd()`     | Absolute path to workspace root, or `${CLAUDE_PROJECT_DIR}` for Claude Code                 |
| `DOC_SEARCH_GLOB`                     | `doc/**/*.md`       | File glob pattern                                                                           |
| `DOC_SEARCH_EXTRA_ROOTS`              | —                   | JSON array of external roots (same shape as `docSearch.extraRoots`)                         |
| `DOC_SEARCH_TRUST_WORKSPACE_SETTINGS` | —                   | `1` to also read the env-only keys from `.vscode/settings.json` (Ollama URL: loopback only) |
| `DOC_SEARCH_HOME`                     | `~/.doc-search`     | Base directory for the global index (absolute path)                                         |
| `DOC_SEARCH_INDEX_LOCATION`           | `global`            | Index location mode: `global` or `workspace` (deprecated)                                   |
| `DOC_SEARCH_INDEX_DIR`                | `.doc-search-index` | Deprecated: workspace-mode index directory; ignored in global mode                          |
| `USE_OPENAI`                          | `0`                 | Set to `1` to use OpenAI embeddings                                                         |
| `OPENAI_API_KEY`                      | —                   | Required if `USE_OPENAI=1`; write it as `${OPENAI_API_KEY}` and export it in your shell     |
| `OLLAMA_URL`                          | —                   | Ollama server URL (presence enables Ollama provider)                                        |
| `OLLAMA_MODEL`                        | `nomic-embed-text`  | Ollama model name                                                                           |

## Shared Index

The MCP server and VS Code extension share the same LanceDB index directory. This means:

- Files indexed by the extension are immediately searchable via MCP
- A reindex triggered via MCP updates the same index the extension uses
- Both must use the same embedding provider — switching providers in one requires a full reindex
