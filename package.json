{
  "name": "mcp-doc-search",
  "displayName": "MCP Doc Search",
  "description": "Semantic documentation search for any monorepo. VS Code extension + MCP server. Local embeddings, no API key required.",
  "version": "0.7.0",
  "publisher": "de-otio",
  "repository": {
    "type": "git",
    "url": "https://github.com/de-otio/mcp-doc-search"
  },
  "homepage": "https://github.com/de-otio/mcp-doc-search#readme",
  "bugs": {
    "url": "https://github.com/de-otio/mcp-doc-search/issues"
  },
  "license": "MIT",
  "keywords": [
    "mcp",
    "documentation",
    "search",
    "embeddings",
    "vector-search",
    "rag",
    "claude",
    "copilot",
    "lancedb",
    "transformers.js"
  ],
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#1f2937",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=18.0.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "workspaceContains:**/*.md"
  ],
  "main": "./dist/extension.js",
  "bin": {
    "mcp-doc-search": "./dist/mcp-doc-search.js"
  },
  "contributes": {
    "commands": [
      {
        "command": "docSearch.search",
        "title": "Doc Search: Search Documentation"
      },
      {
        "command": "docSearch.reindex",
        "title": "Doc Search: Reindex Documentation"
      },
      {
        "command": "docSearch.openSettings",
        "title": "Doc Search: Open Settings"
      },
      {
        "command": "docSearch.generateMcpJson",
        "title": "Doc Search: Generate .mcp.json"
      },
      {
        "command": "docSearch.openWalkthrough",
        "title": "Doc Search: Getting Started"
      },
      {
        "command": "docSearch.openIndexStatus",
        "title": "Doc Search: Index Status"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "docSearch",
          "title": "Doc Search",
          "icon": "media/icon-activity.svg"
        }
      ]
    },
    "views": {
      "docSearch": [
        {
          "id": "docSearch.mainView",
          "name": "Doc Search",
          "contextualTitle": "Doc Search"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "docSearch.mainView",
        "contents": "Semantic documentation search powered by local AI embeddings. No API key required.\n\n[$(book) Getting Started](command:docSearch.openWalkthrough)\n[$(gear) Open Settings](command:docSearch.openSettings)\n[$(database) Index Status](command:docSearch.openIndexStatus)\n[$(search) Search Docs](command:docSearch.search)\n[$(json) Generate .mcp.json](command:docSearch.generateMcpJson)"
      }
    ],
    "walkthroughs": [
      {
        "id": "docSearch.getStarted",
        "title": "Get Started with Doc Search",
        "description": "Set up semantic documentation search in your workspace.",
        "steps": [
          {
            "id": "configureSettings",
            "title": "Configure Settings",
            "description": "Choose your AI model and set which files to index.\n\n[Open Settings](command:docSearch.openSettings)",
            "media": {
              "markdown": "media/walkthrough-settings.md"
            }
          },
          {
            "id": "buildIndex",
            "title": "Build Search Index",
            "description": "Index your documentation so it becomes searchable by meaning.\n\n[Build Index](command:docSearch.openIndexStatus)",
            "media": {
              "markdown": "media/walkthrough-index.md"
            }
          },
          {
            "id": "searchDocs",
            "title": "Search Your Docs",
            "description": "Find relevant documentation using natural language queries.\n\n[Search Docs](command:docSearch.search)",
            "media": {
              "markdown": "media/walkthrough-search.md"
            }
          },
          {
            "id": "mcpIntegration",
            "title": "Use with an AI Assistant",
            "description": "Generate an MCP config so any MCP-compatible AI assistant can search your docs.\n\n[Generate .mcp.json](command:docSearch.generateMcpJson)",
            "media": {
              "markdown": "media/walkthrough-claude.md"
            }
          }
        ]
      }
    ],
    "configuration": {
      "title": "Doc Search",
      "properties": {
        "docSearch.docGlob": {
          "type": "string",
          "default": "doc/**/*.md",
          "description": "Glob pattern for documentation files to index"
        },
        "docSearch.extraRoots": {
          "type": "array",
          "default": [],
          "markdownDescription": "Additional directories **outside the workspace** to index alongside the workspace docs — e.g. a locally cloned vendor-docs repo. Each entry needs a unique `name` (used in `ext://<name>/...` refs) and an absolute `path` (a leading `~` is expanded); `glob` defaults to `**/*.{md,mdx}` and is evaluated inside the root.\n\n**Security note:** every configured root grants MCP/CLI doc-search clients read access to that directory subtree. Review this setting when opening untrusted workspaces (`.vscode/settings.json` is workspace-controlled).\n\nExternal roots are indexed on demand (reindex command / `reindex_docs`); the file watcher only covers the workspace.",
          "items": {
            "type": "object",
            "required": [
              "name",
              "path"
            ],
            "properties": {
              "name": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$",
                "description": "Unique short identifier, used in ext://<name>/... refs"
              },
              "path": {
                "type": "string",
                "description": "Absolute directory path (leading ~ is expanded)"
              },
              "glob": {
                "type": "string",
                "description": "Glob evaluated inside the root (default: **/*.{md,mdx})"
              }
            }
          }
        },
        "docSearch.indexDir": {
          "type": "string",
          "default": ".doc-search-index",
          "description": "Directory to store the vector index (relative to workspace root). Applies in workspace mode only. Setting this to a non-default value implies workspace mode."
        },
        "docSearch.indexLocation": {
          "type": "string",
          "enum": [
            "global",
            "workspace"
          ],
          "default": "global",
          "enumDescriptions": [
            "Store the index under ~/.doc-search (no .gitignore changes needed; an existing .doc-search-index is auto-migrated on first use).",
            "Legacy behaviour: store the index in-tree at the directory specified by indexDir (added to .gitignore automatically)."
          ],
          "markdownDescription": "Where to store the vector index.\n\n- **global** — stored under `~/.doc-search` (or `$DOC_SEARCH_HOME`). No `.gitignore` changes are needed, and an existing `.doc-search-index` in the workspace is automatically migrated on first use.\n- **workspace** — legacy in-tree storage at the path set by `#docSearch.indexDir#`. The directory is added to `.gitignore` automatically."
        },
        "docSearch.headingDepth": {
          "type": "number",
          "default": 2,
          "enum": [
            1,
            2
          ],
          "description": "Split chunks on # only (1) or both # and ## (2)"
        },
        "docSearch.maxChunkChars": {
          "type": "number",
          "default": 4000,
          "description": "Maximum characters per chunk"
        },
        "docSearch.embedProvider": {
          "type": "string",
          "default": "local",
          "enum": [
            "local",
            "ollama",
            "openai"
          ],
          "description": "Embedding provider: local (bundled ONNX model), ollama, or openai"
        },
        "docSearch.ollamaUrl": {
          "type": "string",
          "default": "http://127.0.0.1:11434",
          "description": "Ollama server URL (used when embedProvider is 'ollama')"
        },
        "docSearch.ollamaModel": {
          "type": "string",
          "default": "nomic-embed-text",
          "description": "Ollama model name (used when embedProvider is 'ollama')"
        },
        "docSearch.ollamaAutoRestart": {
          "type": "string",
          "default": "prompt",
          "enum": [
            "never",
            "prompt",
            "auto"
          ],
          "enumDescriptions": [
            "Never restart Ollama; only report the problem",
            "Offer a Restart Ollama button when the server is unusable",
            "Restart Ollama automatically, then retry the reindex once"
          ],
          "markdownDescription": "What to do when Ollama is running but cannot load its model — usually a daemon left running across an upgrade. Restarts use your service manager (`brew services` or user `systemctl`) and never run as root."
        },
        "docSearch.openaiApiKey": {
          "type": "string",
          "default": "",
          "description": "OpenAI API key (used when embedProvider is 'openai')",
          "deprecationMessage": "The OpenAI API key is stored in VS Code's SecretStorage. Set it via the Doc Search Settings panel. This setting is ignored and a value here will be migrated to SecretStorage on next activation."
        },
        "docSearch.autoReindex": {
          "type": "boolean",
          "default": true,
          "description": "Automatically reindex changed markdown files on save"
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.extension.mjs && node esbuild.mcp.mjs && node esbuild.cli.mjs",
    "build:extension": "node esbuild.extension.mjs",
    "build:mcp": "node esbuild.mcp.mjs",
    "build:cli": "node esbuild.cli.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "prettier --check . && eslint src/",
    "lint:eslint": "eslint src/",
    "format": "prettier -w .",
    "package": "npm prune --omit=dev && vsce package; npm install",
    "package:darwin-arm": "npm prune --omit=dev && node scripts/prune-platform-deps.mjs darwin-arm64 && vsce package --target darwin-arm64; npm install",
    "package:darwin-x64": "npm prune --omit=dev && node scripts/prune-platform-deps.mjs darwin-x64 && vsce package --target darwin-x64; npm install",
    "package:linux-x64": "npm prune --omit=dev && node scripts/prune-platform-deps.mjs linux-x64 && vsce package --target linux-x64; npm install",
    "package:win-x64": "npm prune --omit=dev && node scripts/prune-platform-deps.mjs win32-x64 && vsce package --target win32-x64; npm install"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.4.0",
    "@lancedb/lancedb": "^0.13.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/node": "^26.4.1",
    "@types/vscode": "^1.85.0",
    "@vitest/coverage-v8": "^4.1.11",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.2",
    "eslint-plugin-security": "^4.0.1",
    "glob": "^13.0.6",
    "jsonc-parser": "^3.3.1",
    "prettier": "^3.9.6",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.69.0",
    "vitest": "^4.1.9"
  },
  "overrides": {
    "protobufjs": "^7.6.3",
    "@protobufjs/utf8": "^1.1.1",
    "fast-uri": "^3.1.7",
    "postcss": "^8.5.14",
    "flatted": "^3.4.2",
    "@hono/node-server": "^2.0.5",
    "hono": "^4.13.1",
    "js-yaml": "^4.3.1",
    "sharp": "^0.35.0",
    "lodash": "^4.18.0",
    "micromatch": {
      "picomatch": "^2.3.2"
    }
  }
}
