# Configure Settings

Choose how Doc Search processes your documentation:

- **AI model** — Built-in (offline), Ollama (self-hosted), or OpenAI (cloud)
- **File pattern** — Which markdown files to index (e.g. `docs/**/*.md`)
- **Index location** — Where the search database is stored
