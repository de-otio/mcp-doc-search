# Use with Claude Code

Doc Search includes an MCP server that lets Claude Code search your documentation automatically.

- Generate a `.mcp.json` file to register the server with Claude Code
- Claude can then find relevant docs while helping you write code
